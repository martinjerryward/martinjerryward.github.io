
Instructions:
--------------

1) Compile the program `assignment1.c` using the following command:
	   gcc -o assignment1 assignment1.c


2) Run the compiled executable with this command:
   	./assignment1

3) Provide input to the program, ensuring it meets the assignment criteria.

   Sample Input: The quick brown fox jumps over the lazy dog repeatedly without ever getting tired as the sun sets in the sky casting long shadows across the fields while the wind blows gently through the trees and birds sing in harmony creating a peaceful scene that feels like a dream one would wish to never end.

Assumptions / Interpretations / Notes:
---------------------------------------

1) I am experiencing some differences in how my local machine (using CLion) and the server display my output.

2) The program assumes the correct input is provided. The delimiter used for tokenization is a space `" "` and nothing else.

3) Characters, numbers, and punctuation (e.g., `,`) are considered part of the word. 

   Example:
   --------
   ```
   Input: "Pizza,"
   Output: "Pizza,"
   ```

   The punctuation will not be removed, so "Pizza," is treated as one word.

Sample Screenshot:
------------------
A screenshot of the program's input and output is provided as `sample.png`.
